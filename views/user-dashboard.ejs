<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <div class="container">
    <div class="header">
      
      <h1><i class="fas fa-home icon"></i>Welcome, <%= user.name %>!</h1>
      <form action="/logout" method="POST">
        <button type="submit" class="btn">Logout</button>
      </form>
    </div>

    <div class="row">
      <!-- My Profile -->
      <div class="col-lg-6">
        <div class="card">
          <div class="card-header"><i class="fas fa-user icon"></i> My Profile</div>
          <div class="card-body">
            <p>Name: <strong><%= user.name %></strong></p>
            <a href="/profile/edit" class="btn">View Profile</a>
          </div>
        </div>
      </div>

      <!-- Job Applications -->
      <div class="col-lg-6">
        <div class="card">
          <div class="card-header"><i class="fas fa-briefcase icon"></i> Job Applications</div>
          <div class="card-body">
            <p>View available job postings.</p>
            <a href="/jobs" class="btn">View Jobs</a>
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <!-- Applications -->
      <div class="col-lg-6">
        <div class="card">
          <div class="card-header"><i class="fas fa-file-alt icon"></i> My Applications</div>
          <div class="card-body">
            <p>Track all your job applications.</p>
            <a href="/jobs/my-applications" class="btn">View Applications</a>
          </div>
        </div>
      </div>

      <!-- Notifications -->
      <div class="col-lg-6">
        <div class="card">
          <div class="card-header"><i class="fas fa-bell icon"></i> Notifications</div>
          <div class="card-body">
            <p>Get updates about interviews and application statuses.</p>
            <a href="/notifications" class="btn">View Notifications</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
