<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Applications for Job <%= jobId %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4">Applications for Job ID: <%= jobId %></h1>

    <table class="table table-bordered">
      <thead>
        <tr>
          <th>Name</th>
          <th>Email</th>
          <th>CV</th>
          <th>Skills</th>
          <th>Experience</th>
          <th>Education</th>
          <th>Status</th>
          <th>Applied At</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% if (applications.length > 0) { %>
          <% applications.forEach(app => { %>
            <tr>
              <td><%= app.first_name %> <%= app.last_name %></td>
              <td><%= app.email %></td>
              <td><a href="<%= app.cv %>" target="_blank">Download CV</a></td>
              <td><%= app.skills %></td>
              <td><%= app.experience %></td>
              <td><%= app.education %></td>
              <td><%= app.status %></td>
              <td><%= app.applied_at %></td>
              <td>
                <form action="/jobs/applications/<%= app.application_id %>/status" method="POST" class="d-inline">
                  <select name="status" class="form-select d-inline w-auto" onchange="this.form.submit()">
                    <option value="Registered" <%= app.status === 'Registered' ? 'selected' : '' %>>Registered</option>
                    <option value="Invited for an interview" <%= app.status === 'Invited for an interview' ? 'selected' : '' %>>Invited for an interview</option>
                    <option value="Shortlisted" <%= app.status === 'Shortlisted' ? 'selected' : '' %>>Shortlisted</option>
                    <option value="Rejected" <%= app.status === 'Rejected' ? 'selected' : '' %>>Rejected</option>
                  </select>
                </form>
              </td>        
              <td>
                <% if (app.rating) { %>
                  <strong><%= app.rating %></strong>
                <% } else { %>
                  <form action="/jobs/applications/<%= app.application_id %>/rate" method="POST" style="display:inline;">
                    <input type="number" name="rating" min="0" max="100" step="0.01" required>
                    <button type="submit" class="btn btn-success btn-sm">Rate</button>
                  </form>
                <% } %>
              </td>
              <td>
                <a href="/jobs/candidates/<%= app.candidate_id %>" class="btn btn-info btn-sm">Details</a>
              </td>              
            </tr>
          <% }) %>
        <% } else { %>
          <tr>
            <td colspan="9" class="text-center">No applications found for this job.</td>
          </tr>
        <% } %>
      </tbody>
    </table>

    <a href="/admin-dashboard" class="btn btn-primary">Back to Dashboard</a>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
